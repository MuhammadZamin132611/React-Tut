export default function Tut36I(props){
    return (
        <div>
            <span>User {props.data.name}</span>
            <span>User {props.data.email}</span>
            <span>User {props.data.addess}</span>
        </div>
    )
}